#include "main.h"
#include <stdio.h>
/**
 * *_strcpy - copies the string pointed to by src
 *@src : char
 *@dest : char
 *
 * Return: Always 0.
 */
char *_strcpy(char *dest, char *src)
{
char a = 0;
char b = 0;
char c = 0;
char *d:
	
	while (*dest != '\0')
	{
	a++;
	}
	while (*src != '\0')
	{
		b++;
	}
	c = a + b;
	*d = c;
	return (c);
}
