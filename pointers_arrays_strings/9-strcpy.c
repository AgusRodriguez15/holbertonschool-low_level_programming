#include "main.h"
/**
 * *_strcpy - copies the string pointed to by src
 *@src : char
 *@dest : char
 *
 * Return: Always 0.
 */
char *_strcpy(char *dest, char *src)
{
	char *a;
	
	while ((*dest++ = *src++) != '\0')
	{
a = dest;
	}
	_putchar(*a);
	return (a);
}
